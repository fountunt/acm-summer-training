// ============================================================
// 待验证程序 std.cpp —— 你的算法代码
// 与 bf 读同样的输入,输出同样的格式
// ============================================================
#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n, m;
    cin >> n >> m;

    // ... 你的算法 ...

    return 0;
}
