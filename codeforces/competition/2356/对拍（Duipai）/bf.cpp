// ============================================================
// 暴力程序 bf.cpp —— 只求正确,不求效率
// 读与 std 相同的输入,输出相同格式的答案,作为"标准答案"
// ============================================================
#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    // 在这里读入数据,用最直白的方式求答案:
    // 枚举、爆搜、朴素模拟、O(n^2) 都行
    int n, m;
    cin >> n >> m;

    // ... 计算并输出答案 ...

    return 0;
}
